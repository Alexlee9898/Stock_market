# 在 https://finnhub.io/register 注册后复制 API Key（免费档约 60 次/分钟）
#
# 本地：复制本文件为 .env 并填入 FINNHUB_API_KEY=... ；勿把 .env 提交到 Git。
#
# Vercel 线上（否则 /api/finnhub 会返回 503）：
# 1. 打开项目 → Settings → Environment Variables
# 2. 新增 Name 必须为：FINNHUB_API_KEY（区分大小写，不要多空格）
# 3. Value 粘贴 Finnhub 控制台里的 token
# 4. 至少勾选「Production」（若要看 Preview 链接也要勾 Preview）
# 5. Save 后，到 Deployments → 选中最新一条 →「⋯」→ Redeploy（改环境变量后必须重部署才会注入）
#
FINNHUB_API_KEY=
