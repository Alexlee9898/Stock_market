# 行情与日历接口密钥（环境变量名必须为 FINNHUB_API_KEY，与当前后端转发实现一致）
#
# 本地：复制本文件为 .env 并填入 FINNHUB_API_KEY=... ；勿把 .env 提交到 Git。
#
# Vercel 线上（未配置时行情代理会返回 503）：
# 1. 打开项目 → Settings → Environment Variables
# 2. 新增 Name：FINNHUB_API_KEY（区分大小写）
# 3. Value：你在数据服务商控制台获取的 API token
# 4. 至少勾选「Production」（需要 Preview 环境则一并勾选）
# 5. Save 后，到 Deployments → 对最新部署执行 Redeploy（改环境变量后须重部署才会注入）
#
FINNHUB_API_KEY=
